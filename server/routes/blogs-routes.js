const { Router } = require("express");
const route = Router();


route.post("/create/blog", )
route.get("/get/blog", )
route.put("/edit/:id", )
route.delete("delete/:id", )

module.exports = route;